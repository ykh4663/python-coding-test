-- SELECT B.CATEGORY AS CATEGORY, SUM(BS.SALES) AS TOTAL_SALES

-- FROM BOOK B

-- JOIN BOOK_SALES BS

-- ON B.BOOK_ID = BS.BOOK_ID

-- WHERE DATE_FORMAT(BS.SALES_DATE, "%Y-%m-%d") LIKE '2022-01%'

-- GROUP BY B.CATEGORY

-- ORDER BY B.CATEGORY; 


-- SELECT USER_ID, GENDER, DATE_FORMAT(JOINED, '%Y-%m-%d') AS JOINED, CONCAT(SUBSTRING(TLNO, 1, 3),SUBSTRING(TLNO, 5, 4), SUBSTRING(TLNO, 10, 4)) AS TLNO
-- FROM USER_INFO
-- WHERE YEAR(JOINED) = 2021
-- AND TLNO IS NOT NULL
-- ORDER BY USER_ID
--여기서 CONCAT SUBSTRING 조합 안쓰고 REPLACE(TLNO, '-', '') 로 쓰면 더 쉬워짐


SELECT P.PRODUCT_CODE
FROM PRODUCT P
LEFT JOIN OFFLINE_SALE OS
ON P.PRODUCT_ID = OS.PRODUCT_ID
WHERE OS.OFFLINE_SALE_ID IS NULL
ORDER BY P.PRODUCT_CODE;